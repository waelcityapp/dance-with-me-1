const fs = require('fs');

const fetchSource = async () => {
    try {
        const fetch = (await import('node-fetch')).default;
        // Or wait, we can just use `curl` to fetch the source map or JS file and parse it.
    } catch(e) {
    }
}
