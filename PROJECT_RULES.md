# CityEve Project Rules

هذه القواعد ملزمة لأي تعديل لاحق في مشروع CityEve / Dance with Me 1.

## الفروع والنشر

- فرع المعاينة والعمل هو `ai-studio-cityeve`.
- فرع الإنتاج هو `main`.
- ابدأ دائمًا من فرع المعاينة، ولا تعدّل `main` أو تنقل إليه أي تغيير إلا بعد أن يقول صاحب المشروع صراحة: «ارفع على الرئيسية».
- النقل إلى `main` يكون انتقائيًا للتغييرات الموافق عليها فقط، وليس بدمج أو نسخ فرع المعاينة كاملًا.
- لا تُحذف أو تُعاد تهيئة البيانات، التخزين، الاتصالات، الخلفيات، أو إعدادات المشروع القائمة.

## قاعدة الـ API الموحّد — إلزامية

- المشروع يستخدم API Gateway موحّدًا واحدًا. لا تُنشأ ملفات Vercel مستقلة جديدة داخل `api/*.js` لكل ميزة.
- المسارات العامة الحالية مثل `/api/bookings` و`/api/translate` و`/api/marketing-quote` يجب أن تظل تعمل بنفس عناوينها وعقودها مع الواجهة.
- يكون التوجيه داخل الـ Gateway، وتوضع المعالجات والوحدات المساعدة في ملفات داخلية مثل `api/_lib/` بدل زيادة عدد Serverless Functions.
- لا تغيّر أو تحذف مسارًا قائمًا قبل جرد مستهلكيه في الواجهة، وملف `vercel.json`، والـ cron، ومسارات SEO/OG، ثم اختبار التوافق.
- يجب أن يظل عدد الدوال المحتسبة في كل Deployment ضمن حد Vercel Hobby (12 أو أقل). لا نعالج المشكلة بحذف endpoint عشوائي أو كسر وظيفة قائمة.
- أي استثناء لقاعدة الـ API الموحّد يحتاج موافقة صريحة قبل التنفيذ.

## طريقة العمل الإلزامية

1. افحص حالة وفرعي `ai-studio-cityeve` و`main` قبل التعديل، وسجّل الملفات المختلفة والاعتماديات.
2. نفّذ التعديل على المعاينة أولًا.
3. اختبر المسارات المتأثرة، والـ cron، والبناء، وتجربة سطح المكتب والموبايل عند اللزوم.
4. راجع الفرق النهائي كقائمة ملفات؛ لا تنقل إلا الجزء الموافق عليه.
5. بعد نجاح المعاينة، انتظر عبارة «ارفع على الرئيسية» قبل أي تحديث لـ `main` أو نشر Production.
6. عند وجود فشل في Vercel، شخّص سبب الفشل من Deployment نفسه قبل تغيير الكود. خطأ حد الدوال ليس دليلًا على أن الكود يحتاج حذفًا عشوائيًا.

## حالة مرجعية عند كتابة هذه القواعد

- توجد حاليًا فروق بين فرعي المعاينة والإنتاج في ملفات الـ API و`vercel.json`.
- الإنتاج كان يتجاوز حد Vercel Hobby بسبب تعدد ملفات الدوال.
- لا يجوز اعتبار المعاينة والإنتاج متطابقين لمجرد أن المعاينة نجحت؛ يجب مقارنة الملفات والـ commit والـ Deployment صراحة.
