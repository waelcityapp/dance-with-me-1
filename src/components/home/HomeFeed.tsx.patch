--- src/components/home/HomeFeed.tsx
+++ src/components/home/HomeFeed.tsx
@@ -197,6 +197,35 @@
         })}
       </div>
 
+      {/* Why Book Banner */}
+      <motion.div
+        whileHover={{ scale: 1.01 }}
+        whileTap={{ scale: 0.98 }}
+        onClick={() => setShowWhyBookModal(true)}
+        className="relative mb-8 rounded-2xl p-[1.5px] cursor-pointer group overflow-hidden shadow-xl shadow-red-900/10"
+      >
+        {/* Spinning gradient effect */}
+        <div className="absolute inset-[-150%] animate-[spin_3s_linear_infinite]"
+             style={{
+               background: 'conic-gradient(from 0deg, transparent 0 70%, #ef4444 85%, #f59e0b 100%)'
+             }} 
+        />
+        <div className="absolute inset-[-150%] animate-[spin_3s_linear_infinite]"
+             style={{
+               background: 'conic-gradient(from 180deg, transparent 0 70%, #ef4444 85%, #f59e0b 100%)'
+             }} 
+        />
+        
+        {/* Inner Content */}
+        <div className="relative flex items-center justify-between bg-neutral-950 rounded-2xl px-5 py-4 w-full h-full">
+          <div className="flex items-center gap-3">
+            <Sparkles className="h-5 w-5 text-red-500 animate-pulse" />
+            <span className="text-red-500 font-black text-sm sm:text-base tracking-wide drop-shadow-md">
+              {lang === 'ar' ? 'ليه تحجز من خلال التطبيق؟' : 'Why book through the app?'}
+            </span>
+          </div>
+          {lang === 'ar' ? <ChevronLeft className="h-5 w-5 text-red-500 drop-shadow-md" /> : <ChevronRight className="h-5 w-5 text-red-500 drop-shadow-md" />}
+        </div>
+      </motion.div>
+
       {/* Weekly Featured Video Promo (Show on Explore tab when no filter is applied or when all is selected) */}
       {weeklyPromoEvent && selectedCategory === 'all' && !searchQuery && selectedStyleFilter === 'all' && (
         <WeeklyPromoBanner
@@ -207,24 +236,6 @@
       )}
 
-      {/* Why Book Banner */}
-      <motion.div
-        whileHover={{ scale: 1.01 }}
-        whileTap={{ scale: 0.98 }}
-        onClick={() => setShowWhyBookModal(true)}
-        className="relative mb-8 rounded-2xl p-[1px] bg-gradient-to-r from-pink-500 via-indigo-500 to-amber-500 cursor-pointer group"
-      >
-        <div className="absolute inset-0 rounded-2xl bg-gradient-to-r from-pink-500 via-indigo-500 to-amber-500 animate-pulse blur-[2px] opacity-70 group-hover:opacity-100 transition-opacity" />
-        <div className="relative flex items-center justify-between bg-neutral-950/95 backdrop-blur-sm rounded-2xl px-5 py-4 w-full h-full border border-neutral-950">
-          <div className="flex items-center gap-3">
-            <Sparkles className="h-5 w-5 text-red-500 animate-pulse" />
-            <span className="text-red-500 font-black text-sm sm:text-base">
-              {lang === 'ar' ? 'ليه تحجز من خلال التطبيق؟' : 'Why book through the app?'}
-            </span>
-          </div>
-          {lang === 'ar' ? <ChevronLeft className="h-5 w-5 text-red-500" /> : <ChevronRight className="h-5 w-5 text-red-500" />}
-        </div>
-      </motion.div>
-
       {/* Section Header */}
       <div className="flex items-center justify-between border-b border-neutral-800 pb-4">
